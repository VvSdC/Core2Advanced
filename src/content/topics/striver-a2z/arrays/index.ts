import { problemSubTopic } from '../create-problem-subtopic'

export const arraysSubTopic = problemSubTopic({
  id: 'arrays',
  title: 'Arrays',
  description: 'Array problems — two pointers, sliding window, prefix sums, and more.',
})
